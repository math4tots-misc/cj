// test out with:
//   ./run cjx.notc.NCCompiler src/main/notc/sample.notc

int main() {
    int ptr = __malloc(16)
    store!(ptr,      'C')
    store!(ptr + 4,  'A')
    store!(ptr + 8,  'T')
    store!(ptr + 12, 0)
    __print(ptr)
    __free(ptr)
    0
}
