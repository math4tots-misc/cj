package jl1

import jl1.Error

class Assert {
    def(cond: bool) {
        if not cond {
            throw Error("Assertion failed")
        }
    }

    def equal[T](lhs: T, rhs: T) {
        if lhs != rhs {
            throw Error("Expected " + lhs + " to equal " + rhs)
        }
    }
}
