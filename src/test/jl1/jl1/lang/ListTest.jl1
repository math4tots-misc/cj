package jl1.lang

import jl1.Assert
import jl1.IO

class ListTest {

    @test
    def forIn() {
        var total = 0
        for i in [4, 5, 6] {
            total += i
        }
        Assert.equal(total, 4 + 5 + 6)
    }
}
